input {
  beats {
    port => 5046
  }
  
  tcp {
    port => 5044
    codec => json_lines
  }
}

output {
  elasticsearch { 
    hosts => ["es-hot:9200"] 
    index => "logstash-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
